Project Setup and Project Start 
1 . git clone githuble code link
2. npm i
2. check .env if .env comand npm run dev 
4. if not .env setup .env
like :
NODE_ENV=production
PORT=3000
DATABASE_URL=mongodb database conection link
after settup .env 
then comand npm run dev
